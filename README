Decription.
===========

This is an experimental pet project.

At first I just wanted to play with some simple graphics on the cpu,
but then I kind of started doing other things I kind of wanted to do
(e.g. a printf-replacement and custom allocators).

Building.
=========

To build this thing you'll need a c compiler and a posix shell.

$ ./build -O3
